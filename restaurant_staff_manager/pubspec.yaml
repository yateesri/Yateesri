name: restaurant_staff_manager
description: A new Flutter project.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1  # (or latest version)
  image_picker: ^1.0.7  # Make sure this is correctly formatted

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
